#include <iostream>
#include <vector>

using namespace std;

const int maxn = 501;
long long dp[maxn + 1][maxn + 1];

void init_dp()
{
	for (int i = 3; i <= maxn; ++i)
	{
		for (int j = )
	}
}

int main()
{
	init_dp();
	int n;
	cin >> n;
	cout << dp[n][n];
}