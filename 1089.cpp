#include <iostream>
#include <vector>
#include <string>

using namespace std;

int main()
{
	vector <string> dictionary;
	string s;
	while (cin >> s)
	{
		if (s == "#")
			break;
		dictionary.push_back(s);
	}

	string text;
	while (cin >> text)
	{
		vector <string> possible_many_words;

	}
}